<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="view_aebo_birthday_settings_form" model="ir.ui.view">
    <field name="name">aebo.birthday.settings.form</field>
    <field name="model">aebo.birthday.settings</field>
    <field name="arch" type="xml">
      <form string="Birthday Wishes Settings">
        <group>
          <field name="birthday_channel_id" options="{'no_open': True}"/>
          <field name="birthday_include_contacts"/>
        </group>
        <footer>
          <button string="Save" type="object" name="action_save" class="btn-primary"/>
          <button string="Cancel" class="btn-secondary" special="cancel"/>
        </footer>
      </form>
    </field>
  </record>

  <record id="action_aebo_birthday_settings" model="ir.actions.act_window">
    <field name="name">Birthday Wishes Settings</field>
    <field name="res_model">aebo.birthday.settings</field>
    <field name="view_mode">form</field>
    <field name="target">new</field>
    <field name="context">{}</field>
  </record>

  <menuitem id="menu_aebo_birthday_root"
            name="Birthday Wishes"
            parent="mail.mail_menu_root" sequence="60"/>

  <menuitem id="menu_aebo_birthday_settings"
            name="Settings"
            parent="menu_aebo_birthday_root"
            action="action_aebo_birthday_settings"
            sequence="1"/>
</odoo>
