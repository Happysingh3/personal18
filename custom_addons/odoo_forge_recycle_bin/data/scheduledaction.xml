<odoo>
    <data noupdate="1">
        <record id="ir_cron_recycle_bin_clear_job" model="ir.cron">
            <field name="name">Clear Recycle Bin</field>
            <field name="model_id" ref="odoo_forge_recycle_bin.model_recycle_bin"/> <!-- Ensure this reference is correct -->
            <field name="state">code</field>
            <field name="code">model.delete_old_records()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="active" eval="True"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <!-- <field name="numbercall">-1</field> -->
            <!-- <field name="doall" eval="False"/> -->
        </record>
    </data>
</odoo>
